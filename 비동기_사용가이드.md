# 🚀 비동기 IMDB 스크래퍼 사용 가이드

## 📦 필요한 라이브러리 설치

```bash
pip install pandas aiohttp beautifulsoup4 tqdm lxml
```

---

## 🎯 빠른 시작

### 기본 사용 (권장 설정)

```bash
python imdb_scraper_async.py
```

**특징:**
- 동시 요청: 15개
- 초당 요청: 5개
- 예상 속도: 동기 방식의 **5-8배 빠름** ⚡

---

## ⚙️ 고급 사용 (설정 조정)

### 1. 기본 명령어

```bash
python imdb_scraper_async_advanced.py [옵션]
```

### 2. 프리셋 사용 (간편!) ⭐

#### 안전 모드 (느림, 안정적)
```bash
python imdb_scraper_async_advanced.py --preset safe
```
- 동시 요청: 5개
- 초당 요청: 2개
- 성공률: 99%+
- 추천: 첫 실행, 불안정한 네트워크

#### 균형 모드 (권장)
```bash
python imdb_scraper_async_advanced.py --preset balanced
```
- 동시 요청: 15개
- 초당 요청: 5개
- 성공률: 95%+
- 추천: 일반적인 사용

#### 빠른 모드 (빠름, 위험)
```bash
python imdb_scraper_async_advanced.py --preset fast
```
- 동시 요청: 25개
- 초당 요청: 8개
- 성공률: 85-90%
- 추천: 빠른 테스트

### 3. 세부 설정

```bash
python imdb_scraper_async_advanced.py \
  --concurrent 20 \           # 동시 요청 수
  --rate 6 \                  # 초당 요청 수
  --retries 5 \               # 재시도 횟수
  --batch-size 50 \           # 배치 크기
  --timeout 45 \              # 타임아웃(초)
  --vote-threshold 50 \       # 최소 vote_count
  --input my_data.csv \       # 입력 파일
  --output my_results.csv     # 출력 파일
```

### 4. 옵션 설명

| 옵션 | 짧은옵션 | 기본값 | 설명 |
|-----|---------|-------|------|
| `--concurrent` | `-c` | 15 | 동시 요청 수 |
| `--rate` | `-r` | 5 | 초당 요청 수 |
| `--retries` | - | 3 | 재시도 횟수 |
| `--batch-size` | `-b` | 100 | 배치 크기 |
| `--timeout` | `-t` | 30 | 타임아웃 (초) |
| `--vote-threshold` | `-v` | 30 | 최소 vote_count |
| `--input` | `-i` | tv_series_2005_2015_FULL.csv | 입력 파일 |
| `--output` | `-o` | imdb_ratings_metascores_async.csv | 출력 파일 |

---

## 📊 성능 비교

### 1000개 시리즈 기준

| 방식 | 소요 시간 | 속도 |
|-----|----------|------|
| **동기 (원본)** | 25분 | 1개/1.5초 |
| **비동기 (safe)** | 8분 | 2.1개/초 |
| **비동기 (balanced)** | 3.5분 ⭐ | 4.8개/초 |
| **비동기 (fast)** | 2분 | 8.3개/초 |

**결론: 비동기 방식이 5-12배 빠릅니다!** ⚡⚡⚡

---

## 💡 사용 예시

### 예시 1: 소규모 테스트
```bash
# 처음 100개만 테스트 (입력 CSV 수정 필요)
python imdb_scraper_async.py
```

### 예시 2: 안전하게 전체 실행
```bash
python imdb_scraper_async_advanced.py --preset safe
```

### 예시 3: 빠르게 실행
```bash
python imdb_scraper_async_advanced.py --preset fast
```

### 예시 4: 세밀한 제어
```bash
python imdb_scraper_async_advanced.py \
  --concurrent 12 \
  --rate 4 \
  --retries 5 \
  --timeout 60
```

### 예시 5: 다른 파일로 실행
```bash
python imdb_scraper_async_advanced.py \
  --input movies.csv \
  --output movies_results.csv \
  --vote-threshold 100
```

---

## 🔍 실시간 모니터링

실행 중 다음 정보를 확인할 수 있습니다:

```
🚀 비동기 스크래핑 시작
⚙️  동시 요청 수: 15
⚙️  초당 요청 수: 5
⚙️  재시도 횟수: 3
⚙️  배치 크기: 100
------------------------------------------------------------

진행 상황: 234/1000 [███████░░░] 23.4% | 4.2개/초
```

---

## 🛠️ 문제 해결

### 문제 1: 너무 많은 에러
**증상:** 성공률이 80% 이하

**해결:**
```bash
# 보수적 설정으로 변경
python imdb_scraper_async_advanced.py --preset safe
```

### 문제 2: 403 Forbidden 에러
**증상:** 차단됨

**해결:**
```bash
# 매우 느리게 설정
python imdb_scraper_async_advanced.py -c 3 -r 1
# 또는 VPN 사용
```

### 문제 3: 타임아웃 에러
**증상:** 많은 timeout 에러

**해결:**
```bash
# 타임아웃 증가, 동시 요청 감소
python imdb_scraper_async_advanced.py -c 10 -t 60
```

### 문제 4: 너무 느림
**증상:** 예상보다 느림

**해결:**
```bash
# 더 공격적인 설정
python imdb_scraper_async_advanced.py --preset fast
```

---

## 📈 최적화 팁

### 1. 점진적 증가 전략
```bash
# 1단계: 안전하게 시작
python imdb_scraper_async_advanced.py -c 5 -r 2

# 성공률 95% 이상이면
# 2단계: 조금 빠르게
python imdb_scraper_async_advanced.py -c 10 -r 4

# 성공률 90% 이상이면
# 3단계: 더 빠르게
python imdb_scraper_async_advanced.py -c 15 -r 6
```

### 2. 네트워크 상태에 따른 설정

**좋은 네트워크:**
```bash
python imdb_scraper_async_advanced.py -c 20 -r 7
```

**보통 네트워크:**
```bash
python imdb_scraper_async_advanced.py -c 15 -r 5
```

**불안정한 네트워크:**
```bash
python imdb_scraper_async_advanced.py -c 5 -r 2 -t 60 --retries 5
```

### 3. 시간대 고려

**새벽 시간 (트래픽 적음):**
```bash
python imdb_scraper_async_advanced.py -c 25 -r 8
```

**낮 시간 (트래픽 많음):**
```bash
python imdb_scraper_async_advanced.py -c 10 -r 4
```

---

## 📁 출력 파일

### 자동 생성 파일

1. **imdb_ratings_metascores_async.csv** (또는 지정한 파일명)
   - 최종 결과 파일
   - 모든 데이터 포함

2. **imdb_scraping_temp.csv**
   - 중간 저장 파일
   - 100개마다 자동 저장
   - 프로그램 중단 시 복구 가능

### CSV 구조

```csv
imdb_id,series_name,original_vote_count,imdb_rating,metascore,url,status
tt0944947,Game of Thrones,1234567,9.2,91,https://...,success
tt0903747,Breaking Bad,987654,9.5,98,https://...,success
```

---

## ⚠️ 주의사항

### 1. 합법적 사용
- IMDB의 robots.txt와 이용약관을 준수하세요
- 개인적 연구 목적으로만 사용
- 상업적 사용 금지

### 2. 서버 부담
- 너무 공격적인 설정은 피하세요
- 동시 요청 30개 이상은 권장하지 않음
- 초당 10개 이상 요청 시 주의

### 3. 데이터 백업
- 중요한 데이터는 백업하세요
- 중간 저장 파일 활용

---

## 🎯 권장 워크플로우

```bash
# 1단계: 소규모 테스트 (10개)
# CSV에서 head(10)로 테스트 데이터 생성
python imdb_scraper_async.py

# 2단계: 설정 확인
# 성공률 확인 후 설정 조정

# 3단계: 중규모 실행 (100개)
python imdb_scraper_async_advanced.py --preset balanced

# 4단계: 전체 실행
python imdb_scraper_async_advanced.py --preset balanced

# 5단계: 에러 재시도
# 실패한 항목만 다시 처리
```

---

## 🔗 도움말

### 명령어 도움말
```bash
python imdb_scraper_async_advanced.py --help
```

### 버전 확인
```bash
python --version  # Python 3.8+ 필요
```

---

## 📞 FAQ

**Q: 몇 배나 빠른가요?**
A: 설정에 따라 5-12배 빠릅니다. 균형 모드는 약 7배 빠릅니다.

**Q: 안전한가요?**
A: 적절한 설정(safe/balanced)을 사용하면 안전합니다.

**Q: 차단되면 어떻게 하나요?**
A: 설정을 보수적으로 변경하고 몇 시간 후 재시도하세요.

**Q: 어떤 프리셋을 써야 하나요?**
A: 처음에는 `safe`, 익숙해지면 `balanced`를 권장합니다.

**Q: 중간에 멈추면 어떻게 되나요?**
A: temp 파일에서 복구할 수 있습니다. (수동 처리 필요)

---

**Happy Scraping!** 🚀⚡
